# Longest Word Length

## 📝 Description
A simple JavaScript function that finds the length of the longest word in a given sentence.

- The input is a string containing words separated by spaces.
- The output is a number representing the length of the longest word.
- If the string is empty, the function returns `0`.

---

## 💻 Example

```javascript
const sentence = 'JavaScript is really powerful';
console.log(LongestWordLength(sentence)); // Output: 8